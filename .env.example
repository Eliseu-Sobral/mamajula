# ==============================================================================
# Mamajula — variáveis de BUILD do frontend (usadas no build do Vite, no Docker)
# ==============================================================================
#
# IMPORTANTE: Essas variáveis são de BUILD TIME.
# Quando você mudar qualquer valor aqui, PRECISA reconstruir a imagem:
#     docker compose build --no-cache mamajula-app
#     docker compose up -d mamajula-app
#
# Copie este arquivo como ".env" e preencha com seus valores:
#     cp .env.example .env

# ---- Supabase (obrigatórias) ----
# Copie de: Supabase Painel → Project Settings → API
VITE_SUPABASE_URL=https://ytvrtrozkkjsuofevyab.supabase.co
VITE_SUPABASE_ANON_KEY=cole_aqui_sua_anon_key_sem_aspas
